import styled from 'styled-components/native';
import { LinearGradient } from 'expo-linear-gradient';

export const RootContainer = styled(LinearGradient)`
  flex: 1;
  align-items: center;
  justify-content: center;
`;
