import {
	camera,
	key,
	lightning,
	like,
	setting,
	trip,
	user,
	warning,
	download,
	color,
	translate,
	typography
} from '@assets/icons/varied';

export const iconsMenu = {
	camera, key, lightning, like, setting, trip, user, warning, download,
	color, translate, typography
};
